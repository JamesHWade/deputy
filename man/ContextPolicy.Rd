% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context-policy.R
\name{ContextPolicy}
\alias{ContextPolicy}
\title{Configure automatic context management}
\usage{
ContextPolicy(
  max_tokens = 32000L,
  compact_to = 0.5,
  fallback = c("error", "text"),
  max_tool_result_bytes = 64 * 1024,
  offload_dir = NULL
)
}
\arguments{
\item{max_tokens}{Estimated complete-context token threshold that triggers
compaction. Use \code{NULL} to disable automatic compaction.}

\item{compact_to}{Fraction of \code{max_tokens} that the retained recent context
should occupy after compaction.}

\item{fallback}{What to do when LLM summary generation fails. \code{"error"} fails
closed; \code{"text"} uses a deterministic truncated-text summary.}

\item{max_tool_result_bytes}{Serialized size above which a tool result is
stored outside the model context. Use \code{NULL} to disable result offloading.}

\item{offload_dir}{Directory for durable result envelopes. \code{NULL} uses the
Deputy user cache, partitioned by Agent session.}
}
\value{
A \code{ContextPolicy} object.
}
\description{
Defines when an \link{Agent} compacts its conversation and when large tool
results are replaced with durable references. The default policy compacts
before a request would exceed 32,000 estimated tokens and offloads tool
results larger than 64 KiB.
}
