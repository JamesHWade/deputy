% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context-policy.R
\name{ContextPolicy}
\alias{ContextPolicy}
\title{Configure automatic context management}
\usage{
ContextPolicy(
  max_tokens = 32000L,
  compact_to = 0.5,
  fallback = c("error", "text"),
  max_tool_result_bytes = 64 * 1024,
  offload_dir = NULL,
  summary_fallback_chats = list(),
  max_tool_result_image_bytes = 2 * 1024 * 1024,
  max_tool_result_images = 4L
)
}
\arguments{
\item{max_tokens}{Estimated context size, in tokens, that triggers
compaction. \code{NULL} turns automatic compaction off.}

\item{compact_to}{After compaction, the recent turns that are kept take up
about this fraction of \code{max_tokens}. Must be between 0 and 1.}

\item{fallback}{What to do if the model can't write the summary. \code{"error"}
(the default) stops with an error and leaves the conversation unchanged.
\code{"text"} uses a plain summary built from the start of each turn instead.}

\item{max_tool_result_bytes}{Size, in bytes, above which a tool result is
saved to disk and replaced in the model context by a preview and a
reference. Compaction applies the same limit to tool results and tool
call arguments in the turns it summarises. \code{NULL} turns this off.}

\item{offload_dir}{Directory for saved tool results; each session gets its
own subdirectory. A relative path is resolved against the R working
directory when the policy is created. \code{NULL} uses Deputy's user cache
directory.}

\item{summary_fallback_chats}{A list of ellmer Chats to try, in order, if
the agent's own Chat fails to write the summary during automatic
compaction with a transient error. Each must have no turns or tools.
They are only used for summaries and don't change the agent's Chat;
manual \verb{$compact()} doesn't use them.}

\item{max_tool_result_image_bytes}{Maximum total size of the images kept in
one tool result's model context, 2 MiB by default. Inline images count
their encoded size; images given by URL count only the URL. Images over
the limit stay in the saved result. \code{NULL} removes the limit.}

\item{max_tool_result_images}{Maximum number of images kept in one tool
result's model context, 4 by default. \code{0} moves all images out; \code{NULL}
removes the limit. Image limits are separate from
\code{max_tool_result_bytes}.}
}
\value{
A \code{ContextPolicy} object.
}
\description{
Sets when an \link{Agent} compacts its conversation and when large tool results
are moved out of the model context. By default, the agent compacts before a
request would exceed about 32,000 tokens and saves tool results larger than
64 KiB to disk, leaving a short preview and a \verb{deputy://tool-result/...}
reference in the context. The model can read the rest with the
\code{deputy_read_tool_result} tool, and you can with
\link{Agent}\verb{$resolve_tool_result()}.
}
\details{
Automatic compaction runs at the start of a run and between rounds of tool
calls. Summary requests count toward the run's \link{UsageLimits}. The summary is
appended to the system prompt and the model context keeps only the recent
turns; the removed turns stay available from \link{Agent}\verb{$get_turns()} and in
saved sessions. If summarising fails or is cancelled, the conversation is
left as it was. \link{Agent}\verb{$last_compaction()} describes the latest compaction,
including every summary attempt.

The policy is read-only: read fields with \code{$}, and create a new policy to
change one (the example shows how). The Chats in \code{summary_fallback_chats}
are copied, so later changes to your Chat objects don't affect the policy.
Saved sessions don't include the policy; \verb{$load_session()} keeps the
loading agent's policy.
}
\examples{
policy <- ContextPolicy(max_tokens = 16000, fallback = "text")
policy$max_tokens
settings <- S7::props(policy)
settings$max_tokens <- 24000L
do.call(ContextPolicy, settings)
}
