% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-metadata.R
\name{tool_metadata}
\alias{tool_metadata}
\title{Inspect a tool's origin and annotations}
\usage{
tool_metadata(tool)
}
\arguments{
\item{tool}{An ellmer tool, or a provider's built-in tool.}
}
\value{
A list with \code{name}, \code{source}, \code{annotations} (as declared),
\code{missing_annotations}, and \code{effective_annotations} (the declared values,
with cautious defaults filling the gaps). \code{source$type} is \code{"function"},
\code{"package"} (with \code{package}), \code{"provider"}, or \code{"mcp"} (with \code{server} and
\code{tool}). Whether a call is allowed still depends on the agent's
permissions.
}
\description{
Reports where a tool comes from and which annotations it declares. It works
on tools as you created them and on the wrapped copies an agent or subagent
holds. The tool is not called.
}
\examples{
tool_metadata(tool_read_file)
}
\seealso{
\link{PermissionMode}, \link{tools_mcp}, \link{Agent}
}
