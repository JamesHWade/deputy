% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools-mcp.R
\name{tools_mcp}
\alias{tools_mcp}
\title{Get tools from MCP servers}
\usage{
tools_mcp(config = NULL, servers = NULL)
}
\arguments{
\item{config}{Path to an MCP configuration file. Defaults to mcptools'
default location, \verb{~/.config/mcptools/config.json}.}

\item{servers}{Names of the servers to load, matched exactly. Servers not
named are not started. \code{NULL} (the default) loads every configured server.}
}
\value{
A list of tools. If mcptools isn't installed or loading fails,
\code{tools_mcp()} warns and returns an empty list.
}
\description{
Starts the servers in an mcptools configuration file and returns their
tools, ready to register on an \link{Agent}. MCP (Model Context Protocol) servers
give agents tools for services such as GitHub, Slack or Google Drive.
}
\details{
The configuration file uses the Claude Desktop format:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcpServers": \{
    "github": \{
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": \{"GITHUB_TOKEN": "..."\}
    \}
  \}
\}
}\if{html}{\out{</div>}}

Deputy supports mcptools 1.0.2 and 1.0.3; other versions give a warning and
no tools. Annotations a server leaves out get cautious defaults, so MCP
tools usually need \code{web = TRUE} in \code{\link[=Permissions]{Permissions()}}. \code{\link[=tool_metadata]{tool_metadata()}} shows
what a tool declares.

Loading a server again restarts it and breaks the tools from the earlier
load; register the new ones with \code{replace = TRUE}. A stdio server that takes
more than about 4 seconds to answer a call is stopped, and the call fails.
\link{McpConnection} gives you a fixed tool allowlist, timeouts and control over
shutdown.
}
\examples{
\dontrun{
# Get all MCP tools from the default config
mcp_tools <- tools_mcp()

# Create an agent with MCP tools
agent <- Agent$new(
  chat = ellmer::chat("anthropic/claude-sonnet-5"),
  tools = c(tools_file(), mcp_tools),
  permissions = Permissions(web = TRUE)
)

# Use custom config file
mcp_tools <- tools_mcp(config = "path/to/config.json")

# Load tools from specific servers only
mcp_tools <- tools_mcp(servers = c("github", "slack"))
}
}
\seealso{
The \href{https://posit-dev.github.io/mcptools/}{mcptools package} for
configuration, and \code{vignette("mcp")}.
}
