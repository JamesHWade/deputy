# Create a Stop hook result

Return this from a Stop hook callback.

## Usage

``` r
HookResultStop(handled = TRUE)
```

## Arguments

- handled:

  If TRUE, indicates the hook handled the stop event

## Value

A `HookResultStop` object
