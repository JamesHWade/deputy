# Normalize provider name for comparison

Normalize provider name for comparison

## Usage

``` r
normalize_provider_name(provider)
```

## Arguments

- provider:

  Provider name (e.g., "openai", "OpenAI", "anthropic")

## Value

Lowercase normalized provider name, or NA_character\_ if invalid
